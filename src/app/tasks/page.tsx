'use client';
import { useEffect,useState } from 'react'; import Link from 'next/link'; import AppShell from '@/components/AppShell'; import AuthGate from '@/components/AuthGate'; import { api } from '@/lib/api'; import { ClipboardCheck, FolderKanban, ArrowRight, CalendarClock, CheckCircle2 } from 'lucide-react';
type Row={activity_id:string;week_id:string;type:string;title:string;max_score:number;due_at?:string;project_code?:string;submitted?:boolean;status?:string};
export default function TasksPage(){const[rows,setRows]=useState<Row[]>([]);const[error,setError]=useState('');useEffect(()=>{api<Row[]>('listTasks').then(setRows).catch(e=>setError(e.message))},[]);const href=(r:Row)=>r.type==='project'?`/projects/${r.project_code}`:`/tasks/${r.activity_id}`;return <AuthGate><AppShell title="Tugas & Proyek"><div className="section-title"><div><span className="eyebrow">ASESMEN SEMESTER</span><h2>Lima Aktivitas Utama</h2><p className="muted">Pembelajaran Dipandu Mahasiswa, analisis dua artikel, analisis program pelatihan, UTS Curriculum Blueprint, dan UAS Final Training Curriculum. Tidak ada tugas pada setiap pertemuan.</p></div></div>{error&&<div className="error-box">{error}</div>}<div className="stack small-gap">{rows.map(r=><Link href={href(r)} key={r.activity_id} className="glass-card list-card"><div className={`icon-bubble ${r.type==='project'?'coral':'teal'}`}>{r.type==='project'?<FolderKanban/>:<ClipboardCheck/>}</div><div className="grow"><span className="eyebrow">{r.week_id} • {r.type==='project'?'PROJECT':'INDIVIDU'}</span><h3>{r.title}</h3><div className="row wrap gap"><small>Skala 0–100</small>{r.due_at&&<span className="badge"><CalendarClock/>Deadline tersedia</span>}{r.submitted&&<span className="badge success"><CheckCircle2/>Sudah submit</span>}{r.status&&<span className="badge">{r.status}</span>}</div></div><ArrowRight/></Link>)}</div></AppShell></AuthGate>}
