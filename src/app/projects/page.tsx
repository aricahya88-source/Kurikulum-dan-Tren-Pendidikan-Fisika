'use client';
import Link from 'next/link'; import AppShell from '@/components/AppShell'; import AuthGate from '@/components/AuthGate'; import { PROJECTS } from '@/lib/projectThemes'; import { FolderKanban, ArrowRight, UsersRound } from 'lucide-react';
export default function ProjectsPage(){return <AuthGate><AppShell title="Proyek Kurikulum"><div className="section-title"><div><span className="eyebrow">3 WORKFLOW KELOMPOK</span><h2>Dari Fasilitasi ke Desain Kurikulum</h2><p className="muted">Setiap workflow memiliki draft perencanaan → review dosen → revisi → approval → submission final.</p></div></div><div className="project-grid large">{PROJECTS.map(p=><Link className="glass-card project-tile" href={`/projects/${p.code}`} key={p.code}><div className="icon-bubble coral"><FolderKanban/></div><div className="grow"><h3>{p.name}</h3><p>{p.short}</p><span className="badge"><UsersRound/>Kelompok</span></div><ArrowRight/></Link>)}</div></AppShell></AuthGate>}
